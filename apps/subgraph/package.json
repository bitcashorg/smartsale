{
  "name": "smartsale-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "yarn deploy:studio && yarn deploy:hosted-prod",
    "create-local": "graph create --node http://localhost:8020/ smartsale",
    "remove-local": "graph remove --node http://localhost:8020/ smartsale",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 smartsale",
    "test": "graph test",
    "version": "graph --version"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.48.0",
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "version": "0.0.0"
}